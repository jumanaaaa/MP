import { type OrgSchemaAction } from 'botframework-webchat-core';
import React from 'react';
type Props = Readonly<{
    action: OrgSchemaAction;
    onClick?: (action: OrgSchemaAction) => void;
    pressed: boolean;
}>;
declare const FeedbackVoteButton: React.MemoExoticComponent<({ action, onClick, pressed }: Props) => React.JSX.Element>;
export default FeedbackVoteButton;
//# sourceMappingURL=VoteButton.d.ts.map